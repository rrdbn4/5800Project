This assumes you have Java 8 jdk installed on the machine you are trying to compile it on
This also assumes you are running this on a linux or mac distribution

To compile:
  from this directory compile the code using "javac -d . */src.java" in the command line

To run:
  from this directory run the code using "java code.Driver"
